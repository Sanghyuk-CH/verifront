# verifront

프론트엔드 결과물이 맞는지 숫자로 판정하는 CLI.

현재 v0.1에서는 CSS의 색상 값이 디자인 토큰을 지켰는지 검사한다.
육안으로 구분되지 않는 미세한 차이(`#3b82f5` vs `#3b82f6`)까지 잡는 것이 목표다.

개발 중이라 CLI 진입점은 아직 없다. 검사 코어의 동작은 `npm test`로 확인할 수 있다.

MIT